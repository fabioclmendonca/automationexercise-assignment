# Base URL used by e2e (browser) tests. Defaults to https://automationexercise.com if unset.
# BASE_URL=https://automationexercise.com

# Base URL used only by the api project. Defaults to https://automationexercise.com if unset.
# API_BASE_URL=https://automationexercise.com
